import React from 'react';

import { N{{ pascalCase name }} } from '@namespace/index';

// import { secureConsole } from '@secure-console/index'

export const {{ pascalCase children }}: React.FC<N{{ pascalCase name }}.IChildrenComponentProps> = ({ {{ childrenComponentProps }} }) => {
    // HERE YOU CAN DESTRUCTURING DATA OR DO SOMETHING ELSE 
    
    return (  
        <div>
            {{ pascalCase children }} component
        </div>
    );
}