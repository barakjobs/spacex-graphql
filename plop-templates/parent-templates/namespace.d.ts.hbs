export declare namespace N{{ pascalCase name }} {
    export interface IContainerProps {
        {{ containerProps }}: {{{ containerPropsTypes }}}
    }

    export interface IComponentProps {
        {{ componentProps }}: {{{ componentPropsTypes }}}
    }

    // PUT CHILDREN INTERFACE
    
}